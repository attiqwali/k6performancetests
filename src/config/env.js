/*export const ENV = {
  BASE_URL: __ENV.BASE_URL || 'https://api.example.com',
  USERNAME: __ENV.USERNAME || 'testuser',
  PASSWORD: __ENV.PASSWORD || 'password',
};*/

export const ENV = {
  BASE_URL: __ENV.BASE_URL || 'https://reqres.in/api',
};

